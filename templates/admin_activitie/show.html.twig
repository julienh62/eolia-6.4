{% extends 'base.html.twig' %}

{% block title %}Activitie{% endblock %}

{% block body %}
    <h1>Activitie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activitie.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ activitie.title }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ activitie.start ? activitie.start|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ activitie.end ? activitie.end|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ activitie.stock }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ activitie.price }}</td>
            </tr>
            <tr>
                <th>ModifiedPrice</th>
                <td>{{ activitie.modifiedPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_activitie_index') }}">back to list</a>

    <a href="{{ path('app_admin_activitie_edit', {'id': activitie.id}) }}">edit</a>

    {{ include('admin_activitie/_delete_form.html.twig') }}
{% endblock %}
