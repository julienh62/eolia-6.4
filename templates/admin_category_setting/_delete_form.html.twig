<form method="post" action="{{ path('app_admin_category_setting_delete', {'id': category_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_setting.id) }}">
    <button class="btn">Delete</button>
</form>
