{% extends 'base.html.twig' %}

{% block title %}CategorySettings{% endblock %}

{% block body %}
    <h1>CategorySettings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_setting.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ category_setting.title }}</td>
            </tr>
            <tr>
                <th>TextColor</th>
                <td>{{ category_setting.textColor }}</td>
            </tr>
            <tr>
                <th>BorderColor</th>
                <td>{{ category_setting.borderColor }}</td>
            </tr>
            <tr>
                <th>BackGroundColor</th>
                <td>{{ category_setting.backGroundColor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_category_setting_index') }}">back to list</a>

    <a href="{{ path('app_admin_category_setting_edit', {'id': category_setting.id}) }}">edit</a>

    {{ include('admin_category_setting/_delete_form.html.twig') }}
{% endblock %}
