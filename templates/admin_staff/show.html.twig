{% extends 'base.html.twig' %}

{% block title %}Staff{% endblock %}

{% block body %}
    <h1>Staff</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ staff.id }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ staff.fullName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_staff_index') }}">back to list</a>

    <a href="{{ path('app_admin_staff_edit', {'id': staff.id}) }}">edit</a>

    {{ include('admin_staff/_delete_form.html.twig') }}
{% endblock %}
