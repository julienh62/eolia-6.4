{% extends 'base.html.twig' %}

{% block title %}Hello choose cat Controller!{% endblock %}

{% block body %}

    <div class="containerchooseca">
        <h1>Creates Réglagess </h1>

    {% for activiti in activities %}
            <p>
                <a href=" {{ path('app_admin_activitie_setting_new') }} " class="btn btn-success">Créer nouveau reglage de  : {{ activiti.title}}</a>
            </p>

        {% endfor %}


        {% for scheduleStaff in scheduleStaffs %}
            <p>
                <a href=" {{ path('app_admin_scheduleStaffs_setting_new') }} " class="btn btn-success">Créer nouveu reglage de  : {{ scheduleStaff.title}}</a>
            </p>

        {% endfor %}
    </div>


{% endblock %}








